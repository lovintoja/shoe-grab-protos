syntax = "proto3";

service CrmService {
  rpc SendNotificationEmail (SendNotificationEmailRequest) returns (SendNotificationEmailResponse);
}

message SendNotificationEmailRequest {
  string recepientEmail = 1;
}

message SendNotificationEmailResponse {
  bool success = 1;
}